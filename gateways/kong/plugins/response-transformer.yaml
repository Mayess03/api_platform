apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: gateway-source-header
  namespace: kong
  annotations:
    kubernetes.io/ingress.class: kong
plugin: response-transformer
config:
  add:
    headers:
      - "X-Gateway-Source:kong-gateway"