apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: envoy-gateway-tls
  namespace: envoy
spec:
  secretName: envoy-gateway-tls-secret
  dnsNames:
    - envoy.wso2.com
  issuerRef:
    name: wso2-local-ca
    kind: ClusterIssuer